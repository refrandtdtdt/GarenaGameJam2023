using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface PowerUp
{
    // Start is called before the first frame update
    void ApplyEffect(PlayerControl playerControl);
}
